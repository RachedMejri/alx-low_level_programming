#include "main.h"
#include <stdlib.h>

/**
 * _abs - Prints the alphabet in lowercase 10 times
 *
 * @n: al carcter
 *
 * Return: On success, returns 1.
 *         On 0, on error
 */

int _abs(int n)
{

	return (abs(n));

}
