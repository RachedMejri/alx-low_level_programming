#include "main.h"
#include <string.h>

/**
 * _memset - 3abi al plys pil b
 * @s: Pointer to the array of integers.
 * @b: Number of elements in the array.
 * @n: 9dh min w7da
 * Return: Always 0.
 */

char *_memset(char *s, char b, unsigned int n)
{

	memset(s, b, n);

	return (0);
}
