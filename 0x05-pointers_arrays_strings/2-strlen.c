
#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - change the pointer value
 * @s: pointer to the first integer
 * Return: the length of string
 */

int _strlen(char *s)
{

	return (strlen(s));
}
